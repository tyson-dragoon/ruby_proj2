<!--This form should not be shown to users, this data is generated automatically-->

<%= form_with(model: book_history, local: true) do |form| %>
  <% if book_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book_history.errors.count, "error") %> prohibited this book_history from being saved:</h2>

      <ul>
      <% book_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :issueDate %>
    <%= form.date_select :issueDate %>
  </div>

  <div class="field">
    <%= form.label :returnDate %>
    <%= form.date_select :returnDate %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
